# Git-Data Loader Steps
 1. **git init -b main**
 2. **git add .**
 3. **git commit -m "组合导航上位机-界面显示及数据保存"**
 4. **git remote add origin https://github.com/Cloudvale/INS-Host-Computer.git**
 5. **git pull origin main --rebase**
 6. **git push origin main**

## 流程
```bash
git init -b main
git add .
git commit -m "组合导航上位机-界面显示及数据保存"
git remote add origin https://github.com/Cloudvale/INS-Host-Computer.git
git pull origin main --rebase
git push origin main
```
